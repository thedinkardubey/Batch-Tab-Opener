// This is a simple background script.
// You can add your background tasks here if needed.

chrome.runtime.onInstalled.addListener(() => {
    console.log('Bulk Open Sites extension installed');
});
